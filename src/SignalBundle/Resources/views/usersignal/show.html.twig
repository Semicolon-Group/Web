{% block body %}
    <h1>Usersignal</h1>

    <table>
        <tbody>

            <tr>
                <th>Content</th>
                <td>{{ userSignal.content }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('Signal_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('Signal_edit', { 'id': userSignal.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
